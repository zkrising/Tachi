import { integer, Game, Playtypes } from "kamaitachi-common";
import { KtLogger } from "../../../types";

/**
 * Update a user's progress on goals.
 */
async function ProcessUsersGoals(
    game: Game,
    playtypes: Playtypes[Game][],
    userID: integer,
    logger: KtLogger
) {}
