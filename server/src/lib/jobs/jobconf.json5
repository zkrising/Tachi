{
	// this format is just an example - @ereti will be designing the jobs system and will *definitely* want something
	// better. This only exists as a rough example.
	prefix: "tsnode -r tsconfig-paths/register",
	jobs: [{
		path: "ugs-snapshot.ts",
		name: "Snapshot User Game Stats",
		// especially this part - i don't expect any machine to parse this text.
		interval: "daily at midnight"
	}]
}