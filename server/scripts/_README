Hey. If by any chance I'm not available, you can (and should) run these scripts with TS-Node.
https://github.com/TypeStrong/ts-node

They have only been tested for ts-node.

npm install -g ts-node

it's probably installed on the machine you're seeing this on, winter(?). love ya.